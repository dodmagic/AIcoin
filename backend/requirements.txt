# Web framework
fastapi>=0.110
uvicorn[standard]>=0.27

# Data processing
pandas>=2.0
numpy>=1.24

# Classic time-series / stats
statsmodels>=0.14
scikit-learn>=1.3

# HTTP client for exchange data
requests>=2.31

# --- Optional heavy models -------------------------------------------------
# The backend works without these installed (it degrades gracefully and the
# affected models report themselves as unavailable). Install them to enable
# the full set of forecasting models.
#
#   prophet>=1.1        # Facebook Prophet (long-horizon seasonality)
#   xgboost>=2.0        # Gradient boosting on technical-indicator features
#   torch>=2.1          # PyTorch LSTM (deep sequence model)
#   ccxt>=4.0           # Unified exchange data (Binance, etc.)
